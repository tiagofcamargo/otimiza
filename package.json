{
  "name": "otimiza-assessoria-contabil",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@11.22.0",
  "type": "module",
  "engines": {
    "node": "22.x"
  },
  "scripts": {
    "dev": "astro dev --host 0.0.0.0",
    "check": "astro check",
    "build": "astro check && astro build",
    "preview": "node scripts/assert-preview.mjs && astro preview --host 0.0.0.0",
    "test": "node --experimental-strip-types --test tests/*.test.ts",
    "test:browser": "playwright test",
    "start": "node scripts/serve.mjs"
  },
  "dependencies": {
    "astro": "^7.3.3"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.10",
    "@axe-core/playwright": "^4.13.0",
    "@playwright/test": "^1.63.0",
    "@types/node": "^26.6.2",
    "lighthouse": "^13.5.0",
    "typescript": "^6.0.3"
  }
}
